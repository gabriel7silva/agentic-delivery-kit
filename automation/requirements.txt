# Minimal, pinned. Adopters do not need these; maintainers and the CI do.
# pyyaml is often preinstalled by the OS; a floor, not a pin, avoids an uninstall fight.
pyyaml>=6.0
jsonschema==4.23.0
pytest==9.0.3
