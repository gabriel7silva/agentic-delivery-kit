# Current focus

Volatile. Read after `AGENT-CONTEXT.md`, every session. Maintained by {{roles.orchestrator.title}}
or {{roles.product_owner.title}}; updated whenever the active item changes.

Canon: RULE-CONTEXT-VS-TRAIL

## Now

| Field | Value |
|---|---|
| Iteration | <iteration name — pattern `{{cadence.iteration_name_pattern}}`> · goal: <one sentence> |
| Active Feature | <FEATURE-ID> — <title> |
| Active item | <STORY-ID or Task> — <title> — WI-State `<state>` |
| Branch | `<branch>` |
| Handoff to read first | `<path or link>` |
| Scopes claimable | `<scope-id>`, … |

## Recently resolved (awaiting the Approver)

- <ITEM-ID> — <title> — `Resolved` since <date>

## Recently closed

- <ITEM-ID> — <title> — `Closed` <date>

## Out of scope this iteration

What an agent might be tempted to touch and must not.

-

## Open human gates

| Item | Trigger | Waiting on (role) | Since |
|---|---|---|---|
| | | | |

---
*Last updated <date> by <role>. If this is older than the current iteration, it is stale —
ask before trusting it.*
